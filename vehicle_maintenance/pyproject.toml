[project]
name = "vehicle_maintenance"
version = "0.1.0"
description = "Vehicle Maintenance Job Card Platform"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"
