{
  "name": "VM Chat Mention",
  "module": "Fleet Service",
  "doctype": "DocType",
  "istable": 1,
  "editable_grid": 0,
  "engine": "InnoDB",
  "description": "One @mention inside a VM Chat Message. A child row rather than a list packed into a text field, so 'messages that mention me' is an indexed query instead of a LIKE over every body.",
  "fields": [
    {
      "fieldname": "user",
      "label": "User",
      "fieldtype": "Link",
      "options": "User",
      "reqd": 1,
      "in_list_view": 1,
      "columns": 6
    }
  ],
  "permissions": [],
  "sort_field": "idx",
  "sort_order": "ASC"
}
