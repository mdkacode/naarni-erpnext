"""Process RunScan (child table).

One captured component identity — the genealogy row behind where-used containment.
"""

from frappe.model.document import Document


class ProcessRunScan(Document):
	pass
