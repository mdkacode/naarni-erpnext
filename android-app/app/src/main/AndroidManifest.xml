<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Chat attachment uploads run as a dataSync foreground service so a 500 MB
         video survives backgrounding and Doze. Android 14+ requires the typed
         permission as well as the type on the service itself (below). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- Audio for voice notes. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <uses-feature android:name="android.hardware.camera.any" android:required="false" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NaarniService"
        android:usesCleartextTraffic="false">

        <!-- Hands a downloaded chat attachment to whatever app can open it.
             A file:// Uri would throw FileUriExposedException on anything
             modern, so the bytes are shared through a content:// grant. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!--
            Portrait only. Every screen in this app is a form, a checklist or a
            thread, all of which are read down the page; and the people using it
            are holding a phone in one hand with a battery pack in the other. A
            landscape layout nobody designed is worse than no landscape at all.

            `adjustResize` is what keeps the keyboard from covering the field
            being typed into. It only works because the app no longer opts into
            edge-to-edge — see MainActivity and themes.xml.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.NaarniService.Splash"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deeplinks: naarni://ticket/{id} | jobcard/{name} | alert/{id} (plan §8) -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="naarni" android:host="ticket" />
                <data android:scheme="naarni" android:host="jobcard" />
                <data android:scheme="naarni" android:host="alert" />
                <data android:scheme="naarni" android:host="chat" />
                <!-- Handled in MainShell but was missing here, so an external
                     naarni://vehicle/... link never resolved. -->
                <data android:scheme="naarni" android:host="vehicle" />
            </intent-filter>
        </activity>

        <!-- WorkManager's own service, retyped so chunked uploads may run in the
             foreground. tools:node="merge" edits the library's declaration
             rather than replacing it. -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

        <!-- FCM push -->
        <service
            android:name=".core.push.NaarniMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!--
          Inline reply from a chat notification. Not exported: the reply
          PendingIntent has to be MUTABLE so RemoteInput can write the typed
          text into it, and an exported receiver would let any app on the
          device post messages as this user.
        -->
        <receiver
            android:name=".core.push.ChatReplyReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.naarni.service.CHAT_REPLY" />
                <action android:name="com.naarni.service.CHAT_DISMISS" />
            </intent-filter>
        </receiver>

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_notify" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/notification_color" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="job_cards" />
    </application>
</manifest>
